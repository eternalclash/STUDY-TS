let str : string = "Hello"

let num : number = 10;

let arr: Array<number> = [1,2,3];
let heroes: Array<string> = ['Capt','Thor','Hulk',10]
let items: number[] = [1,2,3]  //number로 된 배열

//TS 튜플
let address : [string,number] = ['ganganam',10] ; //문자,숫자만  배열 값을 할당해줘야함
let obj : object = {};

//디테일 객체 속성표현
let person : {name:string, age:number} = {
    name: 'capt',
    age:100,
}
